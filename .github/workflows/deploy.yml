name: Manual container build

on:
  workflow_dispatch:
    inputs:
      publish:
        description: Publish an unsupported, commit-scoped development image to GHCR
        required: true
        default: false
        type: boolean

concurrency:
  group: manual-container-${{ github.ref }}
  cancel-in-progress: false

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  validate:
    name: Validate CLI distribution contracts
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
      - uses: dtolnay/rust-toolchain@4cda84d5c5c54efe2404f9d843567869ab1699d4 # stable
        with:
          toolchain: stable
          components: clippy, rustfmt
      - uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4 # v2.9.1
      - name: Test default CLI
        run: cargo test --locked -p venom-cli
      - name: Check formatting
        run: cargo fmt --all -- --check

  container:
    name: Build non-root CLI container
    needs: validate
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
      - name: Build local image
        run: docker build --tag venom:manual-test .
      - name: Verify container contract
        run: sh tests/distribution/container-contract.sh venom:manual-test
      - name: Log in to GitHub Container Registry
        if: inputs.publish
        uses: docker/login-action@dbcb813823bdd20940b903addbd779551569679f # v4
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Extract commit-scoped image metadata
        if: inputs.publish
        id: meta
        uses: docker/metadata-action@dc802804100637a589fabce1cb79ff13a1411302 # v6
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: type=sha,format=long,prefix=manual-
      - name: Publish commit-scoped development image
        if: inputs.publish
        env:
          IMAGE_TAG: ${{ steps.meta.outputs.tags }}
        run: |
          docker tag venom:manual-test "$IMAGE_TAG"
          docker push "$IMAGE_TAG"
